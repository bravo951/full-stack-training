using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp10
{
    enum Operations
    {
        Add = 1,
        Print,
        Delete,
        Exit
    }

    enum Options
    {
        Department = 1,
        Employee,
        Customer
    }
}
